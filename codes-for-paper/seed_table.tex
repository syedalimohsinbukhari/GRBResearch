% AUTO-GENERATED by codes-for-paper/seed_table_to_latex.py from codes-for-paper/seed_registry.yaml
% Do not hand-edit: regenerate that script and copy the result here.
\begin{table*}[!ht]
    \centering
    \caption{
        Deterministic per-script Monte Carlo seed for every script in this project's analysis pipeline that performs random sampling, each derived by hashing the script's file name together with a master seed of 2828702241.
        The master seed itself was drawn from a cryptographic random-number generator prior to any analysis, so it is auditable and was not chosen to influence any result.
        Deriving each script's seed this way, rather than sharing one literal value, ensures independent analyses draw from independent, uncorrelated random streams while remaining exactly reproducible from this table alone.
    }
    \label{tab:seed_table}
    \resizebox{\textwidth}{!}{
    \begin{tabular}{rll}
        \toprule
        Seed & Item & Fig./Table \\
        \midrule
        2000318690 & Butterfly SEDs (all four bursts) & \cref{fig:butterfly} \\
        2933599809 & Peak energy distribution & \cref{fig:peakenergybestall} \\
        3271127181 & Amati relation & \cref{fig:amati_relationship} \\
        3983203661 & Lorentz factor, Limit A & \cref{fig:gamma_comparison} \\
        4169226666 & Lorentz factor, Limit B & \cref{fig:gamma_comparison} \\
        3315768413 & Photospheric radius / thermal $\Gamma$ & \cref{fig:photospheric,fig:gamma_comparison} \\
        668559939 & Blackbody flux fraction $f_\mathrm{BB}$ & \cref{fig:bbfraction,fig:bbfraction-rest-vs-z} \\
        3624228654 & \ac{GBM}-only refit comparison & near \cref{fig:gbmonly-kt} (not itself \ac{MC}-derived) \\
        2285298891 & \ac{GBM} fluence & \cref{tab:fluence} \\
        \bottomrule
    \end{tabular}
    }
\end{table*}
